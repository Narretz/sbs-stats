node_modules/
dist/
tmp/

# Local env only — never commit this
.env.local
.env.*.local

# .env.production IS committed (no secrets, just the public DB URL)

public/data/
public/vendor/


scripts/__pycache__

# Windows NTFS alternate-data-stream markers
*:Zone.Identifier

# Large GSUA DB — fetched/scraped, not committed
data/general-staff.db